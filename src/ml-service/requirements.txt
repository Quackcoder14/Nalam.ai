# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# LangGraph + LangChain
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-groq>=0.1.9

# Vector Store
# Uses ONNX MiniLM embedding (no PyTorch!) to stay within 512MB RAM on Render free tier
chromadb>=0.5.0
onnxruntime>=1.17.0
tokenizers>=0.15.0

# LLM
groq>=0.9.0

# ML
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
# shap is optional — only used if xai_model.pkl is present on disk
shap>=0.45.0

# HTTP
httpx>=0.26.0
